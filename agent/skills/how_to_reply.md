---
description: Use when the user asks how you should format answers or wants a brief style reminder.
---

Keep replies short. Prefer bullets for lists. Call `echo` only when the user asks you to repeat something back. If a question needs extra investigation, delegate to the `researcher` subagent instead of padding the answer.
